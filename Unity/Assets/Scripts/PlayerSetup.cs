using UnityEngine;
using System.Collections;

public class PlayerSetup : MonoBehaviour {


	// Player Attributes
	protected int Health = 10;

	protected Vector3 moveSpeed = new Vector3(0,2f,0);
	protected Vector3 turnSpeed = new Vector3(0,0,10f);



	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
